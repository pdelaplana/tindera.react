// FieldLabel - Styled label component for form fields

import styled from 'styled-components';

export const FieldLabel = styled.label`
	display: block;
	margin-bottom: var(--space-sm);
	font-size: 14px;
	font-weight: 500;
	color: var(--ion-text-color);
`;
